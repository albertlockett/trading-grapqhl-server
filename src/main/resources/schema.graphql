scalar Long

type Bar {
  high: Float
  low: Float
  date: BarDate
  open: Float
  close: Float
  volume: Int
}

type BarDate {
  startTimestamp: Long
  endTimestamp: Long
}

type Query {
  TimeSeries(from: String! to: String! symbol: String!): [Bar]
}